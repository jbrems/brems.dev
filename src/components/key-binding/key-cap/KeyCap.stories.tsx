import { KeyCap } from "./KeyCap"

export default {
  component: KeyCap,
}

export const L = {
  args: {
    children: 'L',
  }
}

export const Q = {
  args: {
    children: 'Q',
  }
}

export const Esc = {
  args: {
    children: 'esc',
  }
}

export const Ctrl = {
  args: {
    children: 'ctrl',
  }
}

export const Command = {
  args: {
    children: 'command',
  }
}